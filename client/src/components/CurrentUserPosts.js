import React, {useState, useEffect} from 'react'

const CurrentUserPosts = ({user}) => {
    const [myPosts, setMyPosts] = useState(null)

    // useEffect(() => {

    //     fetch("/users").then((r) => {
    //         if (r.ok) {
    //             r.json().then((data) => setAuthors(data));
    //         }
    //     });
    // }, []);


    return (
        <div>
            
        </div>
    )
}

export default CurrentUserPosts
